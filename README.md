# Subtitle Sync Challenge

این پروژه یک پایپ‌لاین کامل برای **پاک‌سازی و همزمان‌سازی زیرنویس‌ها** پیاده‌سازی می‌کند.
زیرنویس ورودی شامل تگ‌های HTML خراب و کاراکترهای اضافی (`+`) است که معمولاً توسط ابزارهای ترجمه‌ی مرورگر ایجاد می‌شوند.

---

## 🎯 Challenge Overview

هدف این چالش:
- حذف HTML و داده‌های خراب از زیرنویس
- همزمان‌سازی زیرنویس‌ها در دو زبان با segmentation متفاوت
- ارائه‌ی خروجی ساخت‌یافته و قابل استفاده

---

## 🐞 Bug Fix Challenge

### مشکل
زیرنویس اصلی شامل موارد زیر بود:
- تگ‌های HTML مانند `<span>`
- ویژگی‌های اضافی مثل `data-sentence`
- وجود کاراکتر `+` در انتهای جملات
- ناخوانا بودن متن نهایی

### راه‌حل
در مرحله‌ی پاک‌سازی:
- تمام تگ‌های HTML حذف شدند
- فقط متن قابل‌خواندن انسان حفظ شد
- کاراکتر `+` به‌صورت صریح حذف شد
- فاصله‌ها و خطوط شکسته نرمال‌سازی شدند

جزئیات کامل در فایل:
`docs/bug_fix_challenge.md`

---

## 🔄 Synchronization Pipeline

به دلیل تفاوت در segment‌بندی زیرنویس‌ها، تطبیق index‑محور قابل اعتماد نبود.
الگوریتم همزمان‌سازی بر اساس هم‌پوشانی تایم‌استمپ‌هاطراحی شد.

جزئیات کامل الگوریتم در:
`docs/synchronization_pipeline.md`

---

## 🧪 تست API

Endpoint:
POST /subtitles/sync/

✅ تست واقعی با PowerShell:
```powershell
Invoke-WebRequest -Uri http://127.0.0.1:8000/subtitles/sync/ `
  -Method POST `
  -ContentType "application/json" `
  -InFile request.json


✅ پاسخ موفق API:
{
  "status": "success",
  "primary_caption_count": 1369,
  "secondary_caption_count": 1718
}

 خروجی نهایی
خروجی نهایی پروژه به‌صورت فایل CSV در مسیر زیر قرار دارد:
output/cleaned_subtitles.csv

